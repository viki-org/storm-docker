name: storm-docker Documentation
baseurl: /storm-docker
markdown: redcarpet
highlighter: pygments

defaults:
  -
    # defaults for all files
    scope:
      path: "" # empty string applies this for all files
    values:
      layout: "default"
